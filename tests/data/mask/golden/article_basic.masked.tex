⟦BLK-0⟧
⟦CAP-0⟧ Deterministic Pipelines for \LaTeX{} Translation
\maketitle

\section{Introduction}
\label{sec:intro}
Working from source keeps every cross reference intact, as $x_i$ and
\cite{knuth1984} show.  ⟦BLK-1⟧
About 50\% of the pipeline is deterministic; the escaped percent above must
survive the round trip.

⟦BLK-2⟧
⟦CAP-1⟧ Pipeline overview
⟦CAP-2⟧ The deterministic pipeline: masking, chunking and the compile loop. Note the escaped \% inside the caption.

Display math becomes an opaque block:
⟦BLK-3⟧

\begin{itemize}
  \item Prose environments stay in the stream.
  \item Their \texttt{\textbackslash begin} wrappers are preserved verbatim.
\end{itemize}

⟦BLK-4⟧
⟦CAP-3⟧ Costs per stage.

\end{document}
